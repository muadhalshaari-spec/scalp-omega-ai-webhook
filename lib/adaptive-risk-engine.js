import{safeDiv,n}from'./quant-core.js';export function buildAdaptiveRisk({entry,direction,atr,structure,liquidity,regime,feesBps=5,slippageBps=2}){const e=n(entry,0),a=Math.max(n(atr,0),e*.0005),buf=a*(regime?.volatility==='EXPANDING'?.28:regime?.volatility==='COMPRESSED'?.12:.2);const invalid=direction==='LONG'?(structure?.protectedLow??e-a):(structure?.protectedHigh??e+a);const sl=direction==='LONG'?Math.min(e-a*.65,invalid-buf):Math.max(e+a*.65,invalid+buf),risk=Math.abs(e-sl),pool=direction==='LONG'?liquidity?.nearestAbove?.price:liquidity?.nearestBelow?.price,ideal=direction==='LONG'?e+risk*2:e-risk*2,tp=Number.isFinite(pool)?direction==='LONG'?Math.max(ideal,Math.min(pool,e+risk*3.2)):Math.min(ideal,Math.max(pool,e-risk*3.2)):ideal,cost=e*(2*feesBps/10000+2*slippageBps/10000);return{entry:e,stopLoss:sl,target:tp,riskDistance:risk,rewardDistance:Math.abs(tp-e),rrAfterCosts:safeDiv(Math.abs(tp-e)-cost,risk),invalidLevel:invalid,buffer:buf,estimatedRoundTripCost:cost}}