import{ema,atr,rsi,slope,percentile,recent,safeDiv}from'./quant-core.js';export function classifyRegime(c){const close=(c||[]).map(x=>x.close),e20=ema(close,20).at(-1),e50=ema(close,50).at(-1),e200=ema(close,200).at(-1),a=atr(c,14),p=close.at(-1),ap=safeDiv(a,p,0)*100,rs=rsi(close,14),s=slope(recent(close,40)),ranges=recent(c,100).map(x=>x.high-x.low),p50=percentile(ranges,.5),p85=percentile(ranges,.85),cr=c.at(-1)?.high-c.at(-1)?.low||0,vol=cr>=p85?'EXPANDING':cr<=p50*.65?'COMPRESSED':'NORMAL';let trend='RANGE';if(e20>e50&&e50>e200&&s>0)trend='UPTREND';else if(e20<e50&&e50<e200&&s<0)trend='DOWNTREND';const environment=vol==='COMPRESSED'?'COMPRESSION':vol==='EXPANDING'?(trend==='RANGE'?'EXPANSION_RANGE':trend):trend==='RANGE'?'MEAN_REVERSION':trend;return{trend,volatility:vol,environment,atr:a,atrPct:ap,rsi:rs,ema20:e20,ema50:e50,ema200:e200,slope:s}}