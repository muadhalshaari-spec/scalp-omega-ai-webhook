export const n=(v,d=null)=>Number.isFinite(Number(v))?Number(v):d;export const clamp=(v,lo=0,hi=1)=>Math.min(hi,Math.max(lo,n(v,lo)));export const safeDiv=(a,b,d=0)=>Number.isFinite(a)&&Number.isFinite(b)&&b!==0?a/b:d;export const mean=a=>a?.length?a.reduce((s,v)=>s+n(v,0),0)/a.length:null;export const sum=a=>(a||[]).reduce((s,v)=>s+n(v,0),0);export const stdev=a=>{if(!a?.length)return null;const m=mean(a);return Math.sqrt(mean(a.map(v=>(n(v,0)-m)**2))||0)};export const percentile=(a,p=.5)=>{const s=(a||[]).map(Number).filter(Number.isFinite).sort((x,y)=>x-y);if(!s.length)return null;const i=(s.length-1)*clamp(p),l=Math.floor(i),h=Math.ceil(i);return s[l]+(s[h]-s[l])*(i-l)};export const zscore=(v,a)=>{const m=mean(a),sd=stdev(a);return m==null||!sd?0:(n(v,0)-m)/sd};export const slope=a=>{const y=(a||[]).map(Number).filter(Number.isFinite),N=y.length;if(N<2)return 0;const xm=(N-1)/2,ym=mean(y);let nu=0,de=0;for(let i=0;i<N;i++){const dx=i-xm;nu+=dx*(y[i]-ym);de+=dx*dx}return de?nu/de:0};export const ema=(a,p)=>{const y=a||[],o=Array(y.length).fill(null);if(y.length<p)return o;let e=mean(y.slice(0,p)),k=2/(p+1);o[p-1]=e;for(let i=p;i<y.length;i++){e=n(y[i],e)*k+e*(1-k);o[i]=e}return o};export const rsi=(a,p=14)=>{if(!a||a.length<=p)return null;let g=0,l=0;for(let i=1;i<=p;i++){const d=a[i]-a[i-1];d>=0?g+=d:l-=d}let ag=g/p,al=l/p;for(let i=p+1;i<a.length;i++){const d=a[i]-a[i-1];ag=(ag*(p-1)+Math.max(d,0))/p;al=(al*(p-1)+Math.max(-d,0))/p}return al===0?100:100-100/(1+ag/al)};export const atr=(c,p=14)=>{if(!c||c.length<=p)return null;const tr=[];for(let i=1;i<c.length;i++){const x=c[i],q=c[i-1];tr.push(Math.max(x.high-x.low,Math.abs(x.high-q.close),Math.abs(x.low-q.close)))}let v=mean(tr.slice(0,p));for(let i=p;i<tr.length;i++)v=(v*(p-1)+tr[i])/p;return v};export const vwap=c=>{let pv=0,v=0;for(const x of c||[]){const t=(x.high+x.low+x.close)/3;pv+=t*x.volume;v+=x.volume}return v?pv/v:null};export const returns=(c,k=1)=>c?.length>k?safeDiv(c.at(-1).close-c.at(-(k+1)).close,c.at(-(k+1)).close,0):0;export const direction=v=>v>0?'BULLISH':v<0?'BEARISH':'NEUTRAL';export const sigmoid=z=>1/(1+Math.exp(-clamp(z,-20,20)));export const correlation=(a,b)=>{const m=Math.min(a?.length||0,b?.length||0);if(m<3)return 0;const x=a.slice(-m),y=b.slice(-m),mx=mean(x),my=mean(y);let u=0,dx=0,dy=0;for(let i=0;i<m;i++){const p=x[i]-mx,q=y[i]-my;u+=p*q;dx+=p*p;dy+=q*q}return dx&&dy?u/Math.sqrt(dx*dy):0};export const recent=(a,k)=>(a||[]).slice(-k);export const utcHour=ts=>new Date(n(ts,0)).getUTCHours();export const timeframeMs=tf=>({'1m':60000,'5m':300000,'15m':900000,'1H':3600000,'4H':14400000,'1D':86400000}[tf]||0);export const normalizeCandle=x=>({time:n(x.time??x[0]),open:n(x.open??x[1]),high:n(x.high??x[2]),low:n(x.low??x[3]),close:n(x.close??x[4]),volume:n(x.volume??x[5],0),confirmed:x.confirmed===true||x.confirmed==='1'||x[8]==='1'});export const hashString=s=>{let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toString(16)}