import{clamp,percentile}from'./quant-core.js';export function deriveThresholds(rows){const p=(rows||[]).map(x=>Number(x.p)).filter(Number.isFinite);if(p.length<50)return{long:.72,short:.72,minEdge:.12,method:'conservative-default'};const q=clamp(percentile(p,.7),.65,.9);return{long:q,short:q,minEdge:clamp(percentile(rows.map(x=>Math.abs((x.p||.5)-.5)),.6),.08,.25),method:'empirical-quantile'}}export function passesThreshold({probability,edge,direction,thresholds}){const t=thresholds?.[direction==='LONG'?'long':'short']??.72;return probability>=t&&edge>=(thresholds?.minEdge??.12)}