import{passesThreshold}from'./threshold-engine.js';export function finalDecision(a){if(!a||a.noTradeReasons?.length||a.decision==='NO_TRADE')return{decision:'NO_TRADE',reason:a?.noTradeReasons||['NO_DATA']};const ok=passesThreshold({probability:a.probability.probability,edge:a.probability.edge,direction:a.decision,thresholds:a.thresholds});return ok?{decision:a.decision,probability:a.probability.probability}:{decision:'NO_TRADE',reason:['FINAL_THRESHOLD']}}