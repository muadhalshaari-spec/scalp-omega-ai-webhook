# SCALP-Ω AI Webhook

Webhook bridge for TradingView alerts.

## Endpoint

`POST /api/webhook`

The first version only verifies that Vercel can receive a webhook. OpenAI integration and authentication will be added after deployment is confirmed.


Institutional engine integration checkpoint.
